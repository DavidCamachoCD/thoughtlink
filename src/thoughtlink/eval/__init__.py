"""Evaluation, ablation and diagnostics utilities for ThoughtLink.

This subpackage groups everything that is *about* the inference layer rather
than part of it: calibration / coverage diagnostics, training-as-a-function
adapters, and the factorial ablation harness used by `scripts/run_ablation.py`.
"""
